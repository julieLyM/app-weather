import React from 'react';

export const City = () => {
  const [city, setCity] = useState([]);

  useEffect(() => {
    const getCity = await getWeatherByCity
    };
  }, [input]);
  return <div></div>;
};
