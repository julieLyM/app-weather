import React from 'react';

export const City = () => {
const [city, setstate] = useState(initialState)

  useEffect(() => {
    effect;
    return () => {
      cleanup;
    };
  }, [input]);
  return <div></div>;
};
