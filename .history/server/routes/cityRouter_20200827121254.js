const router = require('express').Router();

const {
  weatherByCity,
  weatherFiveDays,
} = require('./controllers/weatherCityCtrl');

router.get('/', )
router.get('/city',weathe )


module.exports = router;
